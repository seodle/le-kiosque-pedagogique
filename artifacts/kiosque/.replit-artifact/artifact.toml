kind = "web"
previewPath = "/"
title = "Le Kiosque Pédagogique"
version = "1.0.0"
id = "artifacts/kiosque"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21838

[services.development]
run = "pnpm --filter @workspace/kiosque run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kiosque", "run", "build" ]
publicDir = "artifacts/kiosque/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21838"
BASE_PATH = "/"
